9front's walk, with extras:

-i - do not walk paths matching the regexp:
% walk2 -i '.*/(cache|tmp|\.git)$' /usr

-I (shift i) - Ignore dirs containing a .git dir:
% walk2 -i '.*/(cache|tmp|\.git|src/go|downloads)$' -I /usr

Env var "walk2ignore" can be used to provide a default -i option param.
